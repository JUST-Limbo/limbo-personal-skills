---
description: limbo-personal-skills 目录提交远端时，commit message 默认仅用时间戳；若用户另有文案须在末尾追加同一时间戳
alwaysApply: true
---

# Git 提交说明（limbo-personal-skills）

当用户要求把本目录（`limbo-personal-skills`）的变更**提交并推送到远端**时，**除非用户另有特殊说明**，commit message **直接使用时间戳一行即可**（与 Note 仓库同类规则一致）。

## 时间戳格式（统一）

- **单行片段**：`YYYY年M月D日` + `HH:MM:SS`（24 小时制，**时分秒用英文冒号**，与常见 `git log` 记录一致；月、日等不强制补零）。
- 示例：`2026年4月15日23:43:20`。
- **时间取提交当时**的本地时间（生成 message 时用当前日期时间填入），不要复用旧时间戳。

## 默认与特殊说明并存

- **默认**：整段 commit message **就是**上述时间戳，**不要**再加前缀、括号或额外句子。
- **用户有特殊要求时**（例如指定完整文案、`docs:` / `fix:` 等前缀、中英文混合说明）：以用户给定内容为主体，但**必须在 message 末尾追加一个空格，再跟上与上节相同的时间戳**。
  - 示例：`docs(rules): 补充代码示例约定 2026年4月20日14:05:30`

## 操作注意

- 推送前确认当前仓库根目录为 `limbo-personal-skills`，且 `git status` 中变更符合用户意图。
- 合并、拉取等产生的 merge commit 以 Git 默认说明或用户指定为准；本规则主要针对**用户让代理代为提交的日常提交**。
